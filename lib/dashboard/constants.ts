/** Max preview rows for orders and reviews on member profile dashboard. */
export const MEMBER_DASHBOARD_PREVIEW_LIMIT = 5;

/** Max preview rows for pending orders on merchant profile dashboard. */
export const MERCHANT_DASHBOARD_PENDING_PREVIEW_LIMIT = 4;

/** Max preview rows for reviews on merchant profile dashboard. */
export const MERCHANT_DASHBOARD_REVIEWS_PREVIEW_LIMIT = 3;
