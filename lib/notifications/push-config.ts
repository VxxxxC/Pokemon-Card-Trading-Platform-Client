/** Hours between wishlist price alerts for the same grade row (P-WIS-01). */
export const WISHLIST_PRICE_ALERT_COOLDOWN_HOURS = 24;

/** Hours between chat unread digest pushes per user (P-CHT-01). */
export const CHAT_UNREAD_DIGEST_COOLDOWN_HOURS = 24;

/** Skip P-CHT-01 when user was active within this window (minutes). */
export const CHAT_UNREAD_DIGEST_RECENT_ACTIVITY_MINUTES = 15;

export const PUSH_CRON_BATCH_LIMIT = 50;
