/** Days after ship / payment before fulfillment reminder emails (E-ORD-07/08). */
export const ORDER_FULFILLMENT_REMINDER_AFTER_DAYS = 3;

/** Hours after KYC approve before Connect onboarding reminder (E-MCH-04). */
export const CONNECT_ONBOARDING_REMINDER_AFTER_HOURS = 48;

/** Days before coupon expiry to send E-RWD-02 reminder. */
export const COUPON_EXPIRING_REMINDER_WITHIN_DAYS = 3;

export const ORDER_REMINDER_CRON_BATCH_LIMIT = 50;
