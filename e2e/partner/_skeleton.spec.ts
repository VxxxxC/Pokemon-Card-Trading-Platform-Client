import { test } from "@playwright/test";

/**
 * @partner-id P-SKELETON
 * @features —
 * Placeholder so `test:e2e:partner` resolves the directory.
 * Real specs: p-a01 … p-a08 (Phase P0).
 */
test.describe("Partner regression skeleton", () => {
  test.skip(true, "Replace with P-A01–P-A08 specs per partner-regression.md");
});
